// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://d4nld3v.github.io/astro-portfolio/',
  output: 'static',
  base: '/astro-portfolio/', // Set the base path for GitHub Pages
});
